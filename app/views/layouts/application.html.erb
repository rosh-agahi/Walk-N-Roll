<!DOCTYPE html>
<html>
  <head>
    <title>WalkNRoll</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Coiny&display=swap" rel="stylesheet">
   </head>

  <div class="top_header">
    <div class="title">
      <p>Get ready to </p>
      <h1>    Walk n Roll!</h1>
    </div>
  </div>

  <nav class="menu_bar">
    <% if logged_in? %>
      <div class="menu_items"><%= link_to "Logout", business_owner_logout_path, method: :delete %></div>
      <div class="menu_items"><%= link_to "Home", business_owner_path(session[:business_owner_id]) %></div>
      <div class="menu_items"><%= link_to "Add Client", new_client_path %></div>
      <div class="menu_items"><%= link_to "Add Dog", new_dog_path %></div>
      <div class="menu_items"><%= link_to "Add Dog Walker", new_dogwalker_path %></div>
      <div class="menu_items"><%= link_to "Add Appointment", new_appointment_path %></div>
      <div class="menu_items"><%= link_to "Manage Services", services_path %></div>

    <% else %>
      <%= link_to "Businesss Owner Log In", business_owner_login_path %>
    <% end %>
  </nav>

  <div>
    <%= flash[:notice] %>
  </div>


  <body>
    <%= yield %>
  </body>
</html>
