<h1> Dog Walker: <%= @dogwalker.name %> </h1>



<div class="container">
<div>
  <%= link_to "Delete Dog Walker", "/dogwalkers/#{@dogwalker.id}", method: :delete, :class=>"nav_pills_red link_button" %>
</div>
</div>

<br>

<p><strong>Count of appointments:</strong> <%= @dogwalker.appointments.count %> </p>
<p><strong>Total Tips Earned:</strong> <%= @dogwalker.total_tips_earned %> </p>

<br>

  <table>
    <tr>
      <th>Appointment Date & Time</th>
      <th>Dog Name</th>
      <th>Dog's Owner</th>
      <th>Service</th>
      <th>Tips Earned</th>
    </tr>
    <% @dogwalker.appointments.sort.each do |a| %>
      <tr>
        <td><%= a.display_datetime %></td>
        <td><%= a.dog.name %></td>
        <td><%= link_to a.dog.client.name, client_path(a.dog.client), :class => "table_button" %></td>
        <td><%= a.service.description %></td>
        <td><%= dollar_display(a.tip) %></td>
      </tr>
    <% end %>
  </table>
